# tsc (typescript) lives in devDependencies and is required at build time.
# Some hosts (Hostinger included) set NODE_ENV=production, which makes npm
# default to omitting devDependencies. `include=dev` forces them to install
# anyway; prune with `npm prune --omit=dev` after `npm run build` if a lean
# runtime image matters.
include=dev
