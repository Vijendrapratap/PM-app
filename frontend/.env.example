VITE_API_URL=https://your-backend.up.railway.app/api

# Optional local-development proxy. Use this when the deployed API only
# permits the production frontend origin; browser requests remain same-origin.
VITE_DEV_API_PROXY_TARGET=https://your-backend.up.railway.app
